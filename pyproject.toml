[project]
name = "justpark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.40.30",
    "botocore[crt]>=1.40.30",
    "pydantic>=2.11.7",
]

[dependency-groups]
dev = [
    "ruff>=0.13.0",
]
